# 4060-Rauch — 2026-09-03

Duty: Doelf (Windows-PC) + Grok (Protokoll). Claude: gegenlesen, nicht neu messen.
Ort: lokales `C:\\Users\\aruet\\microduck_rl` — nichts davon in diesem Repo (0 ONNX, 0 STL, 0 Checkpoints).

## Stack

- RTX 4060 8 GB, Treiber 591.86, CUDA-Driver 13.1
- `torch==2.9.1+cu126` nur ueber `.venv\\Scripts\\python.exe`
- `uv run` setzt Lockdatei zurueck auf `2.9.1+cpu` — nicht verwenden
- `numpy==2.4.1` (2.5.2 bricht mediapy: typealias)
- Start: `.\\.venv\\Scripts\\train.exe Mjlab-Velocity-Flat-MicroDuck --env.scene.num-envs 64`
- wandb: Wahl 3, offline

## Lauf

- Log: `logs\\rsl_rl\\velocity\\2026-09-03_06-46-34_velocity`
- Device: `cuda:0`, Warp 1.12, Actor 61→14 (wie Alpha)
- ~76 Iterationen, ~1:19, dann KeyboardInterrupt
- ~1500 Schritte/s bei 64 Envs

| | Iter 0 | Iter 76 |
|---|---|---|
| Mean reward | 0.15 | 0.83 |
| Episode length | ~19 | ~39 |
| Action std | 1.00 | 0.92 |
| fell_over | hoch | ~1.7–2.5 |
| track_linear_velocity | ~0.006 | ~0.03 |

## Urteil

Rauch gruen: die 4060 kann den offiziellen Task lernen.
Das ist kein Gang. 76/50000 bei 64 Envs. Pollen-Richtwert fuer brauchbaren Gang: Stunden bei 4096 Envs.
Mini bleibt bei `alpha_walking.onnx` / `alpha_stand.onnx`.
Kein wandb sync. Kein uv sync auf diesem Env.
